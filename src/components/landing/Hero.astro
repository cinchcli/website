---
// Hero — centered: badge → headline → description → install strip → live demo
import LiveDemo from './LiveDemo.astro';
---

<section class="hero" aria-label="Cinch introduction">

  <!-- Badge -->
  <p class="hero-badge">
    <span class="badge-dot" aria-hidden="true"></span>
    AGPL 3.0 · Open source
  </p>

  <!-- Headline -->
  <h1 class="hero-title">
    Copy from any terminal.
    <span class="title-muted">Paste anywhere.</span>
  </h1>

  <!-- Description -->
  <p class="hero-description">
    Push text, code, or data from any terminal — pull it instantly on any other machine.
    No GUI required.
  </p>

  <!-- Install strip — primary action -->
  <div class="hero-actions">
    <div
      class="install-strip"
      id="install-strip"
      role="button"
      tabindex="0"
      aria-label="Install command — click to copy"
    >
      <span class="dollar">$</span>
      <span class="cmd">curl -fsSL https://cinchcli.com/install.sh | sh</span>
      <button class="copy-btn" id="copy-btn" aria-label="Copy install command" type="button">
        <svg id="icon-copy" width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.75" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
          <rect x="9" y="9" width="13" height="13" rx="2"/>
          <path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1"/>
        </svg>
        <svg id="icon-check" width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="var(--accent)" stroke-width="2.25" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true" style="display:none">
          <polyline points="20 6 9 17 4 12"/>
        </svg>
      </button>
    </div>

    <a href="/docs/getting-started/" class="docs-link">
      View docs →
    </a>
  </div>

  <!-- Interactive live demo -->
  <div class="hero-terminals">
    <LiveDemo />
  </div>

</section>

<style>
  /* ─── Section ───────────────────────────────────────── */
  .hero {
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    text-align: center;
    padding: 5.5rem 2rem 4rem;
    min-height: 100svh;
    max-width: 1100px;
    margin: 0 auto;
    gap: 0;
  }

  /* ─── Badge ─────────────────────────────────────────── */
  .hero-badge {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    font-family: var(--mono);
    font-size: 0.75rem;
    color: var(--text-3);
    letter-spacing: 0.06em;
    margin-bottom: 1.5rem;
    padding: 0.3rem 0.8rem;
    border: 1px solid var(--border-hover);
    border-radius: 100px;
    background: light-dark(rgba(47,127,120,0.06), rgba(79,179,169,0.07));
  }

  .badge-dot {
    width: 6px;
    height: 6px;
    border-radius: 50%;
    background: var(--accent);
    flex-shrink: 0;
    animation: dot-pulse 2.4s ease-in-out infinite;
  }

  @keyframes dot-pulse {
    0%, 100% { opacity: 1; transform: scale(1); }
    50%       { opacity: 0.4; transform: scale(0.72); }
  }

  @media (prefers-reduced-motion: reduce) {
    .badge-dot { animation: none; }
  }

  /* ─── Headline ──────────────────────────────────────── */
  .hero-title {
    /* Use Geist for display: engineered + distinctive, not on blocklist */
    font-family: 'Geist', var(--sans);
    font-size: clamp(2.75rem, 5.5vw, 4.25rem);
    font-weight: 800;
    line-height: 1.06;
    letter-spacing: -0.03em;
    color: var(--text-1);
    text-wrap: balance;
    margin-bottom: 1.25rem;
    /* Disable common ligatures on display weight */
    font-feature-settings: 'liga' 0, 'calt' 1;
    display: flex;
    flex-direction: column;
    gap: 0.1em;
  }

  .title-muted {
    color: var(--text-2);
  }

  /* ─── Description ───────────────────────────────────── */
  .hero-description {
    font-family: var(--sans);
    font-size: 1.0625rem;
    color: var(--text-2);
    line-height: 1.7;
    font-weight: 400;
    letter-spacing: 0.1px;
    max-width: 52ch;
    text-wrap: pretty;
    margin-bottom: 1.75rem;
  }

  /* ─── Actions row ───────────────────────────────────── */
  .hero-actions {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0.875rem;
    margin-bottom: 3rem;
    width: 100%;
  }

  /* Install strip — visual hero of the page */
  .install-strip {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    background: var(--bg-card);
    border: 1px solid color-mix(in srgb, var(--accent) 28%, var(--border-hover));
    border-radius: 10px;
    padding: 0.875rem 1rem 0.875rem 1.25rem;
    font-family: var(--mono);
    font-size: 0.9375rem;
    cursor: pointer;
    transition: border-color 0.2s, background 0.2s, box-shadow 0.2s, transform 0.12s;
    user-select: none;
    touch-action: manipulation;
    width: 100%;
    max-width: 620px;
    box-shadow: 0 1px 2px light-dark(rgba(0,0,0,0.04), rgba(0,0,0,0.18)),
                0 0 0 4px color-mix(in srgb, var(--accent) 8%, transparent);
  }

  @media (hover: hover) {
    .install-strip:hover {
      border-color: var(--accent);
      background: light-dark(#F2F0EC, #131417);
      box-shadow: 0 1px 2px light-dark(rgba(0,0,0,0.06), rgba(0,0,0,0.22)),
                  0 0 0 5px color-mix(in srgb, var(--accent) 14%, transparent);
    }
  }

  .install-strip:active { transform: scale(0.995); }

  .install-strip:focus-visible {
    outline: 2px solid var(--accent);
    outline-offset: 3px;
  }

  .dollar { color: var(--text-3); flex-shrink: 0; }
  .cmd    { color: var(--text-1); flex: 1; min-width: 0; overflow: hidden; text-overflow: ellipsis; white-space: nowrap; }

  .copy-btn {
    display: flex;
    align-items: center;
    padding: 0.25rem;
    border: none;
    background: transparent;
    color: var(--text-3);
    cursor: pointer;
    border-radius: 4px;
    transition: color 0.15s, background 0.15s;
    flex-shrink: 0;
    min-width: 40px;
    min-height: 40px;
    justify-content: center;
  }

  @media (hover: hover) {
    .copy-btn:hover {
      color: var(--text-2);
      background: light-dark(rgba(0,0,0,0.06), rgba(255,255,255,0.06));
    }
  }

  .copy-btn:focus-visible {
    outline: 2px solid var(--accent);
    outline-offset: 2px;
  }

  /* Secondary docs link */
  .docs-link {
    font-family: var(--sans);
    font-size: 0.8125rem;
    color: var(--text-3);
    text-decoration: none;
    letter-spacing: 0.01em;
    padding: 0.25rem 0.5rem;
    border-radius: 4px;
    transition: color 0.15s;
  }

  @media (hover: hover) {
    .docs-link:hover { color: var(--text-2); text-decoration: underline; text-underline-offset: 3px; }
  }

  .docs-link:focus-visible {
    outline: 2px solid var(--accent);
    outline-offset: 3px;
  }

  /* ─── Terminal section — eye-catching supporting visual ─ */
  .hero-terminals {
    width: 100%;
    max-width: 1040px;
    margin: 0 auto;
  }

  .hero-terminals :global(.term-win) {
    box-shadow:
      0 1px 2px light-dark(rgba(0,0,0,0.05), rgba(0,0,0,0.14)),
      0 6px 16px light-dark(rgba(0,0,0,0.07), rgba(0,0,0,0.20)),
      0 28px 60px -16px light-dark(rgba(0,0,0,0.14), rgba(0,0,0,0.42));
  }

  .hero-terminals :global(.term-body) {
    font-size: 0.9375rem;
    line-height: 1.9;
  }

  /* ─── Responsive ────────────────────────────────────── */
  @media (max-width: 1080px) {
    .hero-terminals :global(.term-body) {
      font-size: 0.875rem;
    }
  }

  @media (max-width: 720px) {
    .hero-terminals :global(.term-body) {
      font-size: 0.8125rem;
      line-height: 1.8;
    }
  }

  @media (max-width: 640px) {
    .hero {
      padding: 7rem 1.25rem 4rem;
      min-height: auto;
    }

    .install-strip {
      max-width: 100%;
      padding: 0.75rem 0.875rem 0.75rem 1rem;
      font-size: 0.8125rem;
    }

    .install-strip .cmd {
      font-size: 0.75rem;
    }

    .hero-terminals :global(.term-body) {
      font-size: 0.75rem;
      padding: 0.875rem 1rem 0.75rem;
    }
  }
</style>

<script>
  const strip  = document.getElementById('install-strip');
  const btn    = document.getElementById('copy-btn');
  const copy   = document.getElementById('icon-copy');
  const check  = document.getElementById('icon-check');

  async function copyInstall() {
    try {
      await navigator.clipboard.writeText('curl -fsSL https://cinchcli.com/install.sh | sh');
      if (copy)  copy.style.display  = 'none';
      if (check) check.style.display = 'block';
      setTimeout(() => {
        if (copy)  copy.style.display  = '';
        if (check) check.style.display = 'none';
      }, 2000);
    } catch (_) {}
  }

  btn?.addEventListener('click', (e) => { e.stopPropagation(); copyInstall(); });
  strip?.addEventListener('click', copyInstall);
  strip?.addEventListener('keydown', (e) => {
    const ke = e as KeyboardEvent;
    if (ke.key === 'Enter' || ke.key === ' ') {
      e.preventDefault();
      copyInstall();
    }
  });

</script>
